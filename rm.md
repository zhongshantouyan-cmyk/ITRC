# 📘 ITRC 網站管理員操作手冊

> **中山投研社 CMS（Content Management System）使用指南**
>
> 本文件提供給網站管理人員，說明如何登入後台、更新網站各項內容。

---

## 📑 目錄

1. [系統架構總覽](#系統架構總覽)
2. [管理員登入](#管理員登入)
3. [後台功能總覽](#後台功能總覽)
4. [頁面內容管理](#1-頁面內容管理)
5. [成果發表管理](#2-成果發表管理)
6. [社團成員管理](#3-社團成員管理)
7. [活動管理](#4-活動管理)
8. [參與心得管理](#5-參與心得管理)
9. [修改版面設計 / 程式碼](#修改版面設計--程式碼)
10. [常見問題 FAQ](#常見問題-faq)
11. [外部服務帳號資訊](#外部服務帳號資訊)

---

## 系統架構總覽

| 組件 | 技術 | 部署平台 |
|------|------|----------|
| 前端 (網頁介面) | React + Vite | **Vercel** |
| 後端 (API 伺服器) | Node.js + Express | **Render** |
| 資料庫 | Turso (LibSQL 雲端) | **Turso Cloud** |
| 圖片託管 | Cloudinary | **Cloudinary CDN** |

> 💡 **重點**：網站資料存在 Turso 雲端資料庫，只要登入後台就能隨時隨地修改內容，**不需要動到任何程式碼**。

---

## 管理員登入

### 步驟

1. 打開瀏覽器，前往你的網站網址
2. 在網址後面加上 `/login`（例如：`https://你的網域.vercel.app/login`）
3. 輸入管理員帳號密碼：

   | 欄位 | 預設值 |
   |------|--------|
   | 帳號 | `admin` |
   | 密碼 | `itrc2025` |

4. 點擊「**登入**」按鈕
5. 登入成功後，會自動跳轉到管理後台 (`/admin`)

> ⚠️ **安全提醒**：
> - 登入有效期為 **24 小時**，過期後需重新登入
> - 建議上任後儘速更改預設密碼
> - 請勿將帳號密碼分享給非管理人員

---

## 後台功能總覽

登入後台後，你會在頂部看到 **5 個分頁標籤**：

| 分頁 | 功能說明 | 對應前台區塊 |
|------|----------|-------------|
| 📄 **頁面內容** | 修改首頁各區塊的標題和文字 | 首頁大標題、副標題、課程簡介、課程資訊 |
| 🏆 **成果發表** | 新增/編輯/刪除社團成果 | 成果發表頁面 |
| 👥 **社團成員** | 新增/編輯/刪除幹部與社員名單 | 社團成員區塊 |
| 📅 **活動管理** | 新增/編輯/刪除活動規劃與活動紀錄 | 活動規劃、活動紀錄頁面 |
| 💬 **參與心得** | 新增/編輯/刪除社員心得分享 | 參與心得頁面 |

---

## 1. 頁面內容管理

> 用於修改首頁上顯示的文字內容

### 可修改的區塊

| 區塊 Key | 說明 |
|----------|------|
| `hero_title` | 首頁大標題（例如：中山投研） |
| `hero_subtitle` | 首頁副標題 |
| `course_intro` | 課程簡介段落 |
| `course_info` | 課程資訊（時間、地點、聯絡方式等） |

### 操作步驟

1. 點選「**頁面內容**」分頁
2. 找到你想修改的區塊，點擊右側的「**編輯**」按鈕
3. 修改「**標題**」和「**內容**」欄位
4. 點擊「**儲存**」
5. 看到 ✓ 已儲存 的提示後，回到前台重整頁面即可看到變更

> 📝 **注意**：`course_info` 區塊的內容是 JSON 格式，修改時請保持格式正確：
> ```json
> {
>   "time": "每週五 18:00 - 21:00",
>   "location": "管理學院 CM1037",
>   "coordinators": "張大恒、李沁彤、許筠采",
>   "email": "zhongshantouyan@gmail.com",
>   "ig": "nsysu_itrc"
> }
> ```

---

## 2. 成果發表管理

> 用於管理社團的比賽成績、研究成果等展示

### 新增成果

1. 點選「**成果發表**」分頁
2. 在上方表單填寫：
   - **學期**（必填）：例如 `114-1`
   - **類別**（選填）：例如 `台股`、`美股`、`加密貨幣`
   - **標題**（必填）：成果名稱
   - **說明**（選填）：簡要描述
   - **連結**（選填）：相關資料連結
3. 點擊「**新增**」

### 編輯 / 刪除成果

- 在列表中找到目標項目
- 點「**編輯**」→ 修改表單內容 → 點「**更新**」
- 點「**刪除**」→ 確認對話框中點「確定」即可刪除

---

## 3. 社團成員管理

> 用於管理幹部與社員名單

### 新增成員

1. 點選「**社團成員**」分頁
2. 填寫：
   - **姓名**（必填）
   - **職位**（選填）：例如 `社長`、`副社長`、`公關長`、`社員`
   - **系所**（選填）：例如 `財管系`
   - **年級**（選填）：例如 `大三`
   - **大頭照**（選填）：可直接上傳圖片或貼上圖片網址
3. 點擊「**新增成員**」

### 編輯 / 刪除成員

- 在列表中點「**編輯**」可修改資訊（列表會顯示大頭照縮圖）
- 點「**刪除**」可移除成員

> 💡 **學期交接提醒**：每學期交接時，記得更新幹部名單！

---

## 4. 活動管理

> 用於管理活動規劃（未來活動）與活動紀錄（已完成活動）

### 新增活動

1. 點選「**活動管理**」分頁
2. 填寫：
   - **類型**（必填）：下拉選擇「活動規劃」或「活動紀錄」
   - **日期**（選填）：例如 `114年09月26日(五)`
   - **主題**（必填）：活動名稱
   - **主講人**（選填）：例如 `王昭文 教授`
   - **備註/其他連結**（選填）：支援 Markdown 語法
   - **活動照片**（選填）：見下方上傳說明
3. 點擊「**新增活動**」

### 上傳圖片（活動照片 / 成員大頭照）

所有圖片上傳都使用 **Cloudinary** 雲端託管，有兩種方式：

**方式一：直接上傳圖片**
1. 點擊「選擇檔案」按鈕
2. 選擇電腦中的圖片（上限 10MB）
3. 系統會自動上傳到 Cloudinary 雲端並填入圖片網址
4. 看到 ✓ 圖片上傳成功 即代表完成
5. Cloudinary 會自動幫你壓縮、優化圖片格式，網頁載入更快！

**方式二：貼上圖片網址**
- 直接在圖片網址欄位貼上任何公開圖片的 URL

> ⚠️ 上傳圖片需要設定 Cloudinary API 金鑰，詳見 [外部服務帳號資訊](#外部服務帳號資訊)

### 活動類型說明

| 類型 | 說明 | 對應前台頁面 |
|------|------|-------------|
| `活動規劃 (plan)` | 未來預計舉辦的活動 | `/plans` 活動規劃頁 |
| `活動紀錄 (record)` | 已完成的活動記錄 | `/activities` 活動紀錄頁 |

---

## 5. 參與心得管理

> 用於管理社員的參與心得分享

### 新增心得

1. 點選「**參與心得**」分頁
2. 填寫：
   - **作者**（必填）：撰寫者姓名
   - **標題**（必填）：心得標題
   - **內容**（必填）：心得內文
3. 點擊「**新增心得**」

### 編輯 / 刪除心得

- 與其他模組相同，點「**編輯**」或「**刪除**」即可操作

---

## 修改版面設計 / 程式碼

> ⚡ 以下流程只適用於需要修改**網頁設計、排版或新增功能**的情況
>
> 如果你只是要更新文字、成員、活動等內容，**請使用上面的後台管理**，不需要看這一段。

### 前置準備

確保你的電腦已安裝：
- [Node.js](https://nodejs.org/)（建議 v18+）
- [Git](https://git-scm.com/)
- [VSCode](https://code.visualstudio.com/)（推薦）

### 修改 → 部署流程

```
修改程式碼 → git add . → git commit → git push → 自動部署 ✨
```

#### 詳細步驟：

1. **在 VSCode 中修改檔案**
   - 改顏色/樣式 → 編輯 `client/src/index.css`
   - 改頁面排版 → 編輯 `client/src/pages/*.jsx` 或 `client/src/components/*.jsx`
   - 改後端邏輯 → 編輯 `server/routes/*.js`

2. **存檔後，打開終端機執行 Git 指令**

   ```bash
   git add .
   git commit -m "簡短描述你改了什麼，例如：更新首頁顏色"
   git push
   ```

3. **等待自動部署**
   - **Vercel**（前端）和 **Render**（後端）會自動偵測 GitHub 上的新程式碼
   - 約 **1~2 分鐘**後重新整理網站即可看到更新

> 💡 正常情況下不需要手動去 Vercel/Render 按 Redeploy。只有在 bug 或首次設定失敗時，才需要到後台手動操作。

---

## 常見問題 FAQ

### Q: 忘記管理員密碼怎麼辦？

目前需要透過修改 `server/seed.js` 中的密碼，重新部署來重設。未來可考慮加入密碼重設功能。

### Q: 修改內容後，前台沒有更新？

1. **硬重整**瀏覽器：按 `Cmd + Shift + R`（Mac）或 `Ctrl + Shift + R`（Windows）
2. 確認儲存時有看到 ✓ 已儲存 的提示
3. 確認後端服務 (Render) 是否正常運作

### Q: Render 後端好像休眠了？

Render 免費方案的伺服器閒置約 15 分鐘後會自動休眠，第一次存取時需等待約 30 秒～1 分鐘喚醒。可使用 [cron-job.org](https://console.cron-job.org/jobs) 設定定時 ping 來保持伺服器活躍。

### Q: 圖片上傳失敗？

1. 確認 Render 環境變數中有設定 `CLOUDINARY_CLOUD_NAME`、`CLOUDINARY_API_KEY`、`CLOUDINARY_API_SECRET`
2. 確認圖片大小未超過 10MB
3. 確認網路連線正常
4. 確認 Cloudinary 帳號免費額度尚未用完

### Q: 我可以同時有多個管理員嗎？

目前系統設計為單一管理員帳號。如需多人管理，可共用同一組帳號密碼。

---

## 外部服務帳號資訊

### Cloudinary（圖片上傳用）⭐ 重要

1. 前往 [Cloudinary 官網](https://cloudinary.com/) 註冊免費帳號
2. 登入後，在 Dashboard 首頁就能看到你的 **Cloud Name**、**API Key**、**API Secret**
3. 到 **Render 專案後台** → Environment → Environment Variables
4. 新增以下 3 個環境變數：

   | Key | Value |
   |-----|-------|
   | `CLOUDINARY_CLOUD_NAME` | Dashboard 上的 Cloud Name |
   | `CLOUDINARY_API_KEY` | Dashboard 上的 API Key |
   | `CLOUDINARY_API_SECRET` | Dashboard 上的 API Secret |

5. 儲存後，手動點 **Manual Deploy** 讓設定生效

> 💡 **Cloudinary 免費方案**：每月 25 credits（約 25GB 儲存空間或 25,000 次轉換），小型社團網站綽綽有餘！
> 上傳的圖片會自動壓縮、轉換格式（WebP/AVIF），並透過全球 CDN 加速載入。

### Cron Job（防止後端休眠）

- 管理頁面：[https://console.cron-job.org/jobs](https://console.cron-job.org/jobs)
- 設定每 14 分鐘 ping 一次你的後端 health check endpoint：
  ```
  https://你的後端網址.onrender.com/api/health
  ```

---

## 📂 專案檔案結構參考

```
ITRC/
├── client/                 # 前端 (React + Vite)
│   ├── src/
│   │   ├── pages/
│   │   │   ├── HomePage.jsx        # 首頁
│   │   │   ├── LoginPage.jsx       # 登入頁
│   │   │   ├── AdminPage.jsx       # 管理後台 ⭐
│   │   │   ├── AchievementsPage.jsx
│   │   │   ├── ActivityRecordsPage.jsx
│   │   │   ├── ActivityPlansPage.jsx
│   │   │   └── ExperiencesPage.jsx
│   │   ├── components/             # 共用元件
│   │   ├── context/                # Auth 狀態管理
│   │   ├── api.js                  # API 設定
│   │   ├── index.css               # 全站樣式
│   │   └── App.jsx                 # 路由設定
│   └── vercel.json                 # Vercel 部署設定
├── server/                 # 後端 (Express)
│   ├── routes/
│   │   ├── auth.js                 # 登入認證
│   │   ├── sections.js             # 頁面內容 API
│   │   ├── achievements.js         # 成果發表 API
│   │   ├── members.js              # 社團成員 API
│   │   ├── activities.js           # 活動管理 API
│   │   ├── experiences.js          # 參與心得 API
│   │   └── search.js               # 搜尋 API
│   ├── middleware/auth.js          # JWT 驗證
│   ├── db.js                       # 資料庫連線
│   ├── seed.js                     # 初始資料
│   └── index.js                    # 伺服器入口
├── render.yaml             # Render 部署設定
└── .env                    # 環境變數（不要上傳 GitHub）
```

---

> 📌 **最後提醒**：大部分的網站內容更新（新增活動、修改成員、更新文字等）只需要**登入後台操作**即可，完全不需要碰到程式碼！